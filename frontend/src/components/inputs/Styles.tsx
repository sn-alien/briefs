import { styled } from '@mui/material/styles';
import { Typography } from '@mui/material';

export const Label = styled('label')`
  display: flex;
  span {
    flex: 1;
  }
`;

export const Optional = styled(Typography)`
  color: lightgray;
`;
